apiVersion: v1
kind: Service
metadata:
  name: flaskapp
  labels:
    io.kompose.service: flaskapp
spec:
  type: ClusterIP
  selector:
    io.kompose.service: flaskapp
  ports:
    - name: http
      port: 8000
      targetPort: 80
      protocol: TCP
