apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: markov-app
  labels:
    io.kompose.service: markov-app
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: filestore-rwx
  resources:
    requests:
      storage: 12Gi
