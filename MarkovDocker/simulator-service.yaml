apiVersion: v1
kind: Service
metadata:
  name: simulator

spec:
  type: ClusterIP

  selector:
    app: simulator

  ports:
    - name: http
      port: 80
      targetPort: 80

    - name: api
      port: 4001
      targetPort: 4001
