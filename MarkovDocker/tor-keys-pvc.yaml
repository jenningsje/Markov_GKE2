apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tor-keys
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: filestore-rwx
  volumeName: shared-rwx-pv
  resources:
    requests:
      storage: 1Ti
